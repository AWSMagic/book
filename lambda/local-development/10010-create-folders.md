# Create Workspace Folder

Create `workspace` folder.

```bash
$ cd ~ // switch to the folder you want to work in
$ mkdir workspace
$ WORKSPACE=~/workspace/
$ echo $WORKSPACE
$ /Users/{your_user}/workspace/
```

